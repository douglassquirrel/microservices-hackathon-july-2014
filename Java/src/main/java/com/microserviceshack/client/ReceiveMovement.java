package com.microserviceshack.client;


public class ReceiveMovement {

	public static void main(String[] argv) {
		// while (true) {
		// Connection connection = null;
		// try {
		// // ConnectionFactory factory = new ConnectionFactory();
		// // factory.setHost(Config.RABBIT_MQ_SERVER);
		// //
		// // connection = factory.newConnection();
		// Movement movement = new Movement();
		// connection = movement.getConnection();
		// QueueingConsumer consumer = movement.getConsumer(connection);
		// movement.getSingleMessage(consumer);
		// } catch (Exception e) {
		// e.printStackTrace();
		// } finally {
		// if (connection != null) {
		// try {
		// connection.close();
		// } catch (Exception ignore) {
		// }
		// }
		// }
		// }
	}
}